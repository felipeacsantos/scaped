<p id="notice"><%= notice %></p>

<h1>Esvalparams</h1>

<table>
  <thead>
    <tr>
      <th>Valor</th>
      <th>Esparam</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @esvalparams.each do |esvalparam| %>
      <tr>
        <td><%= esvalparam.valor %></td>
        <td><%= esvalparam.esparam %></td>
        <td><%= link_to 'Show', esvalparam %></td>
        <td><%= link_to 'Edit', edit_esvalparam_path(esvalparam) %></td>
        <td><%= link_to 'Destroy', esvalparam, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Esvalparam', new_esvalparam_path %>
