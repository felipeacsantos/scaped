<p id="notice"><%= notice %></p>

<h1>Criterios</h1>

<table>
  <thead>
    <tr>
      <th>Operador</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @criterios.each do |criterio| %>
      <tr>
        <td><%= criterio.operador %></td>
        <% if can? :read, criterio %>
          <td><%= link_to 'Show', criterio %></td>
        <% end %>
        <% if can? :updade, criterio %>
          <td><%= link_to 'Edit', edit_criterio_path(criterio) %></td>
        <% end %>
        <% if can? :destroy, criterio %>
          <td><%= link_to 'Destroy', criterio, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Criterio', new_criterio_path %>
